import React from "react";

const SectionContianer = ({ children }: { children: React.ReactNode }) => {
  return <div className="max-w-7xl mx-auto my-20">{children}</div>;
};

export default SectionContianer;
